package com.skgtecnologia.sisem.domain.deviceauth.model

enum class DeviceAuthIdentifier {
    DEVICE_AUTH_BUTTON,
    DEVICE_AUTH_CANCEL_BUTTON,
    DEVICE_AUTH_CODE,
    DEVICE_AUTH_SWITCH,
    DEVICE_AUTH_CANCEL_BANNER,
    DEVICE_AUTH_CONTINUE_BANNER,
}
